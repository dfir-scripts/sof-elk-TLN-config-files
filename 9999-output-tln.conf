output {
  if "tln_timeline" in [tags] {
    elasticsearch {
      hosts => ["localhost:9200"]
      index => "tln-%{+YYYY.MM.dd}"
      template_name => "tln_template"
      document_id => "%{[@metadata][fingerprint]}"
    }
  }
}
